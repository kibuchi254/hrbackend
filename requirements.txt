# ========== Core Dependencies ==========
fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
alembic==1.12.1
pydantic==2.5.0
pydantic-settings==2.1.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
python-dateutil==2.8.2
email-validator>=2.1.0
httpx==0.25.2

# ========== Enterprise Dependencies (All Optional) ==========

# Email Notifications (optional - comment out if not using email)
fastapi-mail==1.4.1
email-validator>=2.1.0

# SMS Notifications (optional - depends on provider)
# Kenya SMS Providers (uncomment the one you use):
# africastalking==1.2.1  # Africa's Talking SMS (most popular in Kenya)
# Alternatively, use your preferred SMS provider

# Encryption (required for secure credential vaults)
cryptography>=41.0.0
pycryptodome==3.19.0

# Background Jobs (optional - required if using Celery, comment out if using simple background tasks)
# celery==5.3.6
# redis==5.0.1

# Advanced Analytics (optional - required if using analytics features)
# pandas==2.1.4
# openpyxl==3.1.2

# Reporting (optional - required if using payslip PDF generation)
# reportlab==4.0.9

# Payment Providers (optional - depends on which payment method you use)
# For M-Pesa B2C:
mpesa-sdk==0.4.1

# Rate Limiting (recommended for production)
slowapi==0.1.9

# Audit Logging
python-json-logger==2.0.7
